# Objective

Implement papers and create a performant LLM

## Architecture

- In each part (P_i), a folder = a paper / concept (the "Final" folder is the result of all the papers)
- All implemented papers are described in `history.md`
- The followed guideline is described in `roadmap.md` (AI generated)

the actual best model is in P_max/Final/